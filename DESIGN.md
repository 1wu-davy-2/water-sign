# 设计思路

这份文档记录**为什么这么写**，而不是写了什么。代码能说明「是什么」，但下面这些取舍
在半年后回看时，光读代码是看不出来的。

---

## 1. 产品定位

一个**纯前端**的水象星座（巨蟹 / 天蝎 / 双鱼）恋爱沟通指南。

核心判断：这类内容的瓶颈不在「写不出来」，而在**用的时候找不到**。
用户真正的使用场景是——聊天框已经打开了，对方刚回了一句冷淡的话，他需要**现在立刻**
找到一句能发出去的话。

这个场景决定了三个设计目标，后面几乎所有取舍都围绕它们：

1. **检索优先于阅读。** 首页第一屏是搜索框，不是目录。
2. **话术必须可复制。** 找到之后一步就能用，不能让他手动选中文本。
3. **按情绪场景组织，而不是按知识体系组织。** 用户想的是「他冷淡了怎么办」，
   不是「第二章第三节」。

---

## 2. 信息架构

### 为什么是「模块（散文）+ 话术库（结构化）」双轨

内容是两种完全不同的东西，硬塞进一种结构里必然别扭：

| | 教学正文 | 话术模板 |
| --- | --- | --- |
| 形态 | 长文，有论证和递进 | 短句，独立可用 |
| 使用方式 | 从头读 | 检索 / 筛选 / 复制 |
| 需要的能力 | 排版、层级、可读性 | 标签、筛选、收藏、一键复制 |
| 存放 | `modules.ts`（Markdown 字符串） | `speech-templates.ts`（结构化记录） |

所以话术**没有**留在 Markdown 里，而是抽成了带 `scene` / `zodiac` / `tags` / `why`
字段的记录。代价是模块 2 和模块 5 里原本内联的话术列表要改成引用
（`Section.relatedTemplates`），收益是复制按钮、场景筛选、星座筛选、搜索、收藏
五套功能全部建立在**同一份数据**上，不会出现「改了正文忘了改话术库」。

### 为什么正文用 Markdown，而不是结构化段落

试过把每个段落也结构化（`{type: 'h3' | 'p' | 'li', text}`），但教学正文里有大量
嵌套列表、引用块、表格（模块 5 的雷区对比表），结构化 schema 会迅速膨胀成一个
自己实现的 Markdown——不如直接用 Markdown。

代价是**必须小心模板字符串的缩进**：Markdown 把 4 个空格以上的缩进当代码块，
所以 `modules.ts` 里的正文一律顶格写。这一点在文件头和 README 里都标了警告。

### 星座标签的语义

`zodiac: ['all']` 表示三座通用，`['cancer', 'pisces']` 表示只对这两座适用。

筛选「巨蟹」时，**通用话术也会出现**（排在专属话术之后）。因为筛选器回答的是
「这条话术对巨蟹**是否适用**」，通用话术当然适用。如果只显示专属的，用户会以为
内容缺失。

这里踩过一个坑，值得记下来：筛选器最初只有一个 `'all'` 值，同时承担了
「不筛选」和「通用标签」两个含义，结果**默认状态下 39 条话术只显示了 24 条**，
15 条星座专属的被静默隐藏，界面上没有任何提示。现在拆成了 `any`（不限）和
`all`（通用）两个独立状态。

> 教训：一个筛选器的默认值如果是「某个具体取值」，那它就没法同时表达「不筛选」。
> 这两种状态必须分开。

---

## 3. 检索设计

### 为什么不用 Fuse.js

全站内容 39 条话术 + 21 个小节，压缩前不到 100KB。内存里建索引线性扫描，
单次查询是微秒级。引入模糊搜索库要付出体积、配置和不可控排序的代价，
换来的能力在这个量级上完全用不上。

模块加载时构建一次索引（模块级常量），组件里用 `useMemo` 缓存「查询 → 结果」。

### 打分

按字段权重累加：标题 12 / 关键词 8 / 场景与星座标签 6 / 正文 3（加命中次数，上限 5）。
全部不中时退回字符级子序列匹配（`冷谈` 也能命中 `冷淡`），给 1 分兜底。

### 多个检索词之间是 AND

这一条最初**做错了**：正文检索是 AND，话术检索却写成了 OR，导致搜
「天蝎 zzz」返回 7 张卡片，而页面上一个高亮都没有——用户看到一堆完全不含关键词的
结果，只会认为搜索坏了。现在两边统一为 AND。

> 教训：同一个功能的两条代码路径，语义必须显式对齐。这里连文档注释都写了
> 「多个检索词之间是 AND」，但只写在了其中一个函数上。

### 检索面必须覆盖界面上可见的文字

话术的检索文本最初只有正文 + 标签 + `scene` 的**英文 key**，没有场景和星座的
**中文标签**。后果是：用户在筛选器上看到「冷淡应对」四个字，照着搜，一条都搜不到。

现在把 `SCENE_META[...].label` 和 `ZODIAC_META[...].label` 也拼进了检索文本。

同理，分词器必须切开**中文标点和全角括号**。正文里大量使用「已读不回」这种写法，
用户复制粘贴过来搜索时，如果 `「」` 不被切掉，整个 `「已读不回」` 会当成一个检索词，
结果为零。

> 教训：检索的输入来自用户看到的界面，所以**界面上出现的每一种文字形态都得能被搜到**。

---

## 4. 状态管理

### 收藏为什么不用 Context

收藏状态被三类组件同时读取：卡片（星标）、顶栏（计数徽章）、收藏页。
用 Context 的话需要一个 Provider 包住整棵树，且任何一次收藏都会让**整棵子树重渲染**。

改用模块级迷你 store + `useSyncExternalStore`：只有真正订阅的组件重渲染。
`getSnapshot` 返回模块级的 `favorites` 数组，只在写入时整体替换——保证引用稳定，
否则每次 `getSnapshot()` 返回新数组会让 React 无限重渲染。

数据只写 `localStorage`，不上传。读写全部包了 try/catch：隐私模式下 `localStorage`
会直接抛异常，不能让它把整个页面带崩。

> 已知取舍：39 张卡片**都**订阅了这个 store，所以点一次星标仍然会重渲染 39 张卡。
> 在这个量级上无感；如果内容涨到几百条，需要改成按 id 订阅。

---

## 5. 视觉设计

**深色单主题**，不做浅色切换。水象的意象是深水，浅色版会彻底丢掉这个调性；
而且做双主题意味着每个颜色都要验证两遍对比度，收益不划算。

配色用 Tailwind v4 的 `@theme` 定义成语义令牌（`abyss` 背景层次 / `tide` 主色 /
`lilac` 副色 / `mist` 点缀 / `foam` 文字），而不是散落的十六进制值。

### 三个具体的坑

**背景光晕画在了不透明背景下面。** `body::before` 用 `z-index: -1` 做固定光晕，
但按 CSS 绘制顺序，负 z-index 的定位元素画在**根元素背景之上、body 背景之下**。
底色留在 `body` 上时，光晕被 `body` 自己的不透明背景完全盖住。
修法是把底色挪到 `html`，让 `body` 透明。

**星座符号渲染成了彩色方块。** `♋ ♏ ♓`（U+2648–U+2653）在 Unicode 里
**默认是 emoji 表现**，浏览器会用彩色 emoji 字体渲染，和正文小字号完全不搭。
在符号后面追加 U+FE0E（文本表现选择符）强制走文本字形。

**最弱一级的文字色对比度不合格。** `--color-foam-500` 原来是 `#6b7899`，
在纯底色上刚好 4.55:1（勉强过 AA），但一放到卡片表面、下拉面板、`abyss-800` 上
就掉到 3.95–4.41:1，全部不合格——而这些恰恰是它实际被使用的地方。
改成 `#7d8aab` 后是 5.04–5.82:1。

> 教训：对比度要在**元素实际所处的背景**上算，不是在页面底色上算。

---

## 6. 两个必须记住的工程约束

**`document.querySelector(hash)` 会让整站白屏。** 从 URL 片段滚到锚点时，
片段不保证是合法 CSS 选择器——`#1`、`#-1`、`#1a`、`#%` 都会让 `querySelector`
抛 `SyntaxError`。在 `useEffect` 里抛错，React 19 会卸载整棵树，页面变成纯白，
且没有任何恢复入口。

修法是 `getElementById(decodeURIComponent(hash.slice(1)))` 加 try/catch。
另外补了顶层 `ErrorBoundary` 作为兜底——任何未捕获的渲染异常都不该让用户看到白屏。

**`react-router-dom` 在 v8 已被移除。** 包名是 `react-router`。两个同时安装会
解析出两份 React context，运行时抛 `useNavigate() may be used only in the context
of a <Router>`。另外 `react-router@8` 要求 Node ≥ 22.22，**比 Vite 自己的下限还高**，
所以 `package.json` 里显式写了 `engines`。

---

## 7. 已知边界

- 收藏只存在浏览器 `localStorage`，换设备即丢失，没有后端。
- 检索是内存线性扫描，内容量级到几百 KB 以上应换成预建索引（Pagefind 等）。
- 搜索框不是真正的 `combobox`（没有 `aria-activedescendant`），
  移动端抽屉没有焦点陷阱——键盘用户 Tab 仍可走到抽屉背后的内容。
- 内容仅供娱乐与自我觉察参考，不构成心理或情感咨询建议。
